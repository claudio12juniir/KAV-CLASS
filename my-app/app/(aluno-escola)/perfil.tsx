// Perfil (Rede Social — Social/Perfil, INSTITUTION Fase 1): mesma tela do
// SELF, reaproveitada aqui em vez de duplicar, igual ao padrão já usado por
// reels.tsx/feed.tsx.
import PerfilAluno from '../(aluno)/perfil';

export default function PerfilAlunoEscola() {
  return <PerfilAluno />;
}
